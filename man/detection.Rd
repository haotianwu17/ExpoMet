% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detection.R
\name{detection}
\alias{detection}
\title{Calculate Feature Detection Rates}
\usage{
detection(
  data,
  ID_col,
  feature_cols = NULL,
  non_detect_values = NULL,
  lod_table = NULL,
  drop = FALSE,
  threshold = 0.5
)
}
\arguments{
\item{data}{A data frame or matrix containing samples in rows and variables
in columns. Matrix input is converted to a data frame.}

\item{ID_col}{A single non-empty character string identifying the sample ID
column. This argument must be provided explicitly.}

\item{feature_cols}{An optional character vector containing the names of
feature columns. When \code{NULL}, all columns except \code{ID_col} are treated as
feature columns. If \code{ID_col} is included, it is removed with a warning.}

\item{non_detect_values}{Optional values that should be counted as
non-detects, such as \code{0}, \code{"0.00"}, \code{"ND"}, or \code{"<LOD"}.
Numeric values are matched by value, so \code{0}, \code{"0"}, and \code{"0.00"} are
treated the same. To count missing R values as non-detects, include
an actual \code{NA}. If \code{NA} is not included, missing R values are skipped
when calculating the detection rate.}

\item{lod_table}{An optional data frame containing columns named \code{feature}
and \code{lod}. The \code{feature} column identifies feature names and the \code{lod}
column provides finite feature-specific LOD values. Negative LOD values
are converted to zero with a warning. Additional columns are permitted
and ignored.}

\item{drop}{A single logical value indicating whether features should be
removed according to their detection rates. When \code{FALSE}, detection rates
are calculated without filtering. The default is \code{FALSE}.}

\item{threshold}{A single numeric value between 0 and 1 used when
\code{drop = TRUE}. Features with detection rates below this value, or with
detection rates that cannot be calculated, are removed. Features with
detection rates equal to the threshold are retained. The default is
\code{0.5}.}
}
\value{
A named list containing:

\itemize{
\item \code{data}: The cleaned data, optionally excluding dropped features.
\item \code{detection_rates}: Feature-level LODs, evaluable counts, detected
counts, detection rates, and filtering actions.
\item \code{summary}: Numbers and proportion of retained and removed features.
\item \code{data_issue}: Recorded data-quality messages.
\item \code{kept_features}: Names of retained features.
\item \code{dropped_features}: Names of removed features.
}
}
\description{
Calculates feature-level detection rates using user-defined non-detect
values, feature-specific limits of detection (LODs), or both. The function
can optionally remove features whose detection rates are below a specified
threshold.
}
\details{
At least one of \code{non_detect_values} or \code{lod_table} must be provided.

When both are provided, a value is classified as detected only when it is
not a user-defined non-detect and is greater than or equal to its
feature-specific LOD.

Actual \code{NA} values not included in \code{non_detect_values} are treated as
ordinary missing observations and excluded from the detection-rate
denominator.

Negative feature measurements are converted to zero with a warning.
Negative LOD values are converted to zero with a warning.

Duplicated input column names are reduced to their first occurrence.
Duplicated sample IDs are reduced to their first row. Both operations
produce warnings.
}
\examples{
example_data <- data.frame(
  sample_id = paste0("S", 1:4),
  feature_1 = c(0, 1.2, 2.1, NA),
  feature_2 = c(0, 0, 3.4, 4.2)
)

detection(
  data = example_data,
  ID_col = "sample_id",
  non_detect_values = 0
)

detection(
  data = example_data,
  ID_col = "sample_id",
  non_detect_values = 0,
  drop = TRUE,
  threshold = 0.5
)

}
